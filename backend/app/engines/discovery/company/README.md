"""Company Discovery Engine — construction-estimating lead finder.

Discovers companies from public web sources (Google, Bing), validates
their websites, removes duplicates, and returns structured candidates
ready for downstream research.

This engine uses NO AI — only HTTP requests and rule-based filtering.
"""
